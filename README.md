# 行舟 · 个人行动与生活系统

行舟是一个由插件独立维护数据的思源个人行动与生活系统。“项目与事务”管理长期领域、项目、任务和想法；“生活节律”记录科研工作、身体状态、个人生活与恢复。两类数据都保存在插件内部，不要求用户另建数据库或文档来承载。

> 当前版本：`0.6.0`（项目事务与生活节律双模块、独立存储、轮换备份与写后复核）

[English README](README.en.md) · [中文更新日志](CHANGELOG.md) · [English Changelog](CHANGELOG.en.md)

## 核心能力

- 一级模块分为“项目与事务”和“生活节律”；“全部／本周／收件箱／整理”只属于项目与事务，“今日记录／历史数据／评分标准／时间线”只属于生活节律；
- 生活节律支持科研工作日、周六轻量复盘、周日半日科研和休假／节假日四种日期类型；按星期给出默认值，并允许逐日覆盖；
- 每日表单支持小时＋分钟输入睡眠时长、kg/lb 体重单位、小时与分钟下拉时间、工作边界自动判定、分阶段填写和内嵌评分规则；昨晚熄灯与今日起床会保存为明确的跨日日期时间；
- 下班后的工作闭环入口明确区分“尚未确认／不需要／需要”；只有选择“需要”才展开内容与时长字段，“不需要”的时长按不适用保存而不是记为 0；
- 休假日不显示科研字段，工作指标按“不适用”处理，不会作为未完成或零分进入后续分析；
- “全部”页浏览长期领域、顶层项目、子项目、任务、事务和想法的完整层级，并在今日工作项及其上层路径显示“今日”与汇总数量；同级工作项可通过每行的上移、下移按钮或拖动手柄手动排序；
- “本周”页按计划开始日到截止日期连续展示跨日工作项，并标明开始、持续、截止及跨周承接状态；可直接安排、移动和取消安排。每张日期卡片独立记录当日完成，其他日期及工作项整体状态不受影响，完成记录可撤销且在项目整体结束后仍保留于历史周；
- “收件箱”支持仅填写名称的低摩擦捕获；
- “整理”页提供收件箱、活跃项目、遗留日期、行动细则和本周结果五步检查；
- 直接编辑名称、类型、状态、上层工作项、计划开始日、截止日期、时长、精力与行动内容；“本次行动细则”和“下一步行动”在阅读态支持基础思源 Markdown 渲染，编辑框会按完整内容自动展开、自动续写列表，并在输入、删除、剪切或粘贴后立即校正同级编号；
- “完成后开始”和“需先行”可连接任意领域或项目中的工作项，并阻止自依赖和循环依赖；
- 可选关联思源文档，但独立工作项不需要建立文档；
- 删除工作项时保留下级工作项，并自动清除其他工作项对它的层级及依赖引用。

## 数据保存与迁移

完整工作项、同级顺序和逐日完成日期由插件私有的 `work-items.json` 保存。每次新增、编辑、排序、排期、逐日完成、整体完成或删除时，行舟都会：

1. 在写入前把上一版轮换保存到三个内部备份文件之一；
2. 写入新的完整数据；
3. 重新读取并逐项比对，确认保存结果一致后才向界面返回成功；
4. 如果主数据无法识别，优先从最新的有效内部备份恢复；如果主数据和备份都损坏，则停止写入，避免静默覆盖。

从 `0.4.x` 升级且尚未存在内部数据时，插件会读取设置中指定的旧属性视图一次，连同旧版插件私有的跨项目依赖一起迁移，并保存迁移快照。迁移完成后，日常运行不再读取或修改旧属性视图。

因此，确认升级后能正常看到所有工作项以后，删除旧数据库或承载数据库的文档不会删除行舟内部的工作项。需要注意：如果某个工作项只保存了一个指向思源文档的关联，而该文档本身被删除，那么工作项仍在，但对应文档自然无法再打开。

本次架构改造前另有一份通过思源本地属性视图接口创建的人工快照，仅保留在开发者本机，不进入源码仓库或安装包，也不参与插件运行。

生活节律使用独立的 `daily-records.json`，首次打开时建立空仓库，不读取也不迁移“改变2026·每日数据”或其他思源文档。它与工作项仓库分别维护修订号、三份轮换备份和写后复核。

## 生活节律读取接口

插件实例为后续统计或 AI 分析保留两个只读入口：

- `getDailyRecordsSnapshot({ from?, to? })`：读取完整记录或指定日期范围的不可变快照；
- `getDailyRubrics()`：读取当前版本的五套评分规则。

记录会保留日期类型和评分规则版本，因此休假日可从工作指标统计中排除，历史数据也不会被未来规则调整误读。

## 安装测试包

1. 下载构建产生的 `package.zip`。
2. 在思源工作空间的 `data/plugins/` 下创建 `siyuan-plugin-xingzhou` 文件夹。
3. 将压缩包内容解压到该文件夹，确保 `plugin.json` 位于根目录。
4. 重启思源或重新加载插件，在集市已下载插件中启用“行舟”。
5. 从旧版升级时，第一次打开后核对工作项数量、层级和依赖；设置中的两个旧数据库 ID 仅用于这一次导入。

## 本地开发

```bash
pnpm install
pnpm test
pnpm check
pnpm build
```

`pnpm build` 会生成 `dist/` 和可安装的 `package.zip`。

## 数据与隐私

- 行舟不包含远程服务；
- 完整工作项、每日记录、迁移快照和轮换备份均由思源的插件私有数据机制保存；
- 只有打开已关联文档以及首次旧版迁移需要访问思源本地内容；
- 插件不会自动创建或删除思源文档；
- `disabledInPublish` 已启用，避免在发布服务中加载个人管理界面。

## 许可证

[MIT](LICENSE)
