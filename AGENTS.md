# 行舟插件开发规则

## 沟通方式

- 默认使用中文回答。
- 修改代码前，先说明问题原因、准备采用的方案以及影响范围。
- 未经用户明确指示开始修改（例如“开始修改”“动手吧”），不要修改源码、测试或文档；评估、建议阶段只输出分析与设计。
- UI 相关改动先用真实代码渲染设计稿（复用 `dist/index.css` 与组件标记结构生成对比页面）供用户确认，用户确认 UI 后再实现。
- `design/` 目录是 UI 设计中间产物，永不入库、不入任何 commit；推送前检查，不要误提交。
- 工作过程中提供简短进度；完成时汇报修改、验证、部署和 Git 状态。

## 修改原则

- 开始前检查 `git status --short`，保留用户已有修改，不覆盖、回退或清理无关文件。
- 修改代码前先确认本机思源笔记版本（当前为 v3.8.3；可从 `/Applications/SiYuan.app` 的 `Info.plist` 读取）；若查看版本被环境权限阻止，先向用户请求权限。
- 结合当前思源版本评估其新 API 或特性能否直接应用于插件，可复用或更优时再决定是否采纳，不能只按旧版本思路实现。
- 优先使用 `rg` 查找代码和文件。
- 使用 `apply_patch` 修改源码；格式化工具或构建生成物除外。
- 只修改当前需求涉及的代码，不做无关重构。
- 修复缺陷时补充有意义的回归测试，覆盖用户描述的实际操作路径和相关边界条件。
- 不直接编辑 `dist/`、`dev/` 或 `package.zip`；这些内容必须由构建流程生成。
- 安装依赖时默认遵循现有锁文件；只有任务明确要求调整依赖时才更新依赖及锁文件。

## 代码职责边界

- `src/index.ts` 负责插件生命周期、界面入口、持久化编排和旧数据迁移入口。
- `src/internal-store.ts`、`src/daily-records.ts`、`src/checklist.ts`、`src/nutrition.ts` 等领域模块负责解析、规范化、版本升级和纯数据操作。
- Svelte 组件负责交互与展示；不要在组件中另建一套持久化流程或复制领域规则。
- 思源 API 请求统一通过已有 API 封装处理错误；新增接口前先检查 `src/siyuan-api.ts` 和现有调用方式。
- 测试文件应与源码职责对应；能在纯函数层验证的规则，不只依赖组件测试覆盖。

## 数据与思源安全

- 修改 `work-items.json`、`daily-records.json`、`checklist.json` 或 `nutrition.json` 的结构和保存流程时，必须保留以下不变量：
  - 修订号单调递增；
  - 写入主文件前轮换保存上一版；
  - 写入后重新读取、解析并完整比对；
  - 主文件损坏时选择修订号最高的有效备份恢复；
  - 主文件和备份均不可识别时停止写入，禁止用空数据静默覆盖；
  - 写操作继续经过统一的串行队列，避免并发保存互相覆盖。
- 数据结构变化必须兼容已发布版本的存量数据，并为解析、迁移、非法数据、备份和恢复增加测试。
- 操作思源文档时，必须使用思源本地 API 的块级、属性视图或数据库接口定位并修改明确目标；禁止把 Markdown 导出结果作为回写源，禁止整体覆盖文档根块。
- 批量修改思源内容前，先通过 SQL、`getChildBlocks`、`getBlockDOM` 或相应数据库读取接口确认目标；写入后通过非导出接口复核。
- 需要恢复思源文档时，优先使用思源历史接口定位并恢复准确版本。

## 测试、构建与本机部署

完成代码修改后，按风险依次执行：

1. 运行直接相关的测试：
   `CI=true pnpm vitest run <相关测试文件>`
2. 运行 Svelte 和类型检查：
   `CI=true pnpm run check`
3. 对涉及业务逻辑、数据或多个组件的修改运行完整测试：
   `CI=true pnpm test`
4. 生成生产构建：
   `CI=true pnpm build`
5. 只有测试、检查和构建通过后，才运行已有部署脚本：
   `CI=true pnpm run deploy:local`
6. 部署脚本必须比较以下文件在 `dist/` 和实际插件目录中的 SHA-256 校验值：
   - `index.js`
   - `index.css`
7. 提醒用户重新加载或重启行舟插件进行实际验证。

部署时禁止使用 `rsync --delete`，禁止删除或覆盖插件目录中的用户数据文件。构建或依赖命令遇到非交互确认时使用 `CI=true`；若因沙箱网络限制失败，按权限流程请求运行，不要绕过安全限制。

测试范围按修改类型选择：

- 纯领域逻辑：运行对应单元测试。
- Svelte 交互：运行相关组件测试及其依赖的领域测试。
- 数据格式或持久化：覆盖旧数据、非法数据、修订号、备份、恢复和写后复核。
- 日期与排期：覆盖本地日期、跨日、星期边界和 UTC 边界。
- 布局与弹窗：检查宽屏和窄屏、滚动容器、焦点与关闭行为。
- 修改公共行为或多个模块：运行完整测试。

## 完成汇报

完成修改后明确说明：

- 问题根因和实际修改；
- 影响范围；
- 运行过的测试及通过数量；
- 类型检查的错误和警告数量；
- 构建是否成功；
- 是否已部署到本机思源插件目录；
- 当前是否存在未提交改动。

## Git 与发布

除非用户明确要求，否则：

- 不创建 Git commit；
- 不推送 GitHub；
- 不修改版本号；
- 不创建 Git 标签或 Release。

README 与 CHANGELOG 维护：

- 面向用户的功能或行为变化，主动同步更新中文与英文 README、CHANGELOG；不影响用户可见行为的改动由我自行判断（通常只记 CHANGELOG 或都不记）。
- 用户明确要求更新 README、CHANGELOG 或 Release Notes 时，必须更新。
- 更新时机：测试全部通过后、准备推送时再更新 README/CHANGELOG；测试不通过时不更新文档，避免返工。

用户明确要求“推送”时，先检查差异和验证结果，再创建内容明确的提交并推送 `main`。不要把无关的用户改动混入提交。

用户明确要求“release”时：

- 更新 `package.json` 和 `plugin.json` 中的版本；
- 更新中英文 README、CHANGELOG 和 Release Notes；
- Release Notes 使用中英文双语；
- 完整执行检查、测试、构建和安装包校验；
- 通过推送版本标签触发 GitHub Actions 发布；
- 不使用浏览器手动创建 Release；
- 发布后核验 Actions 状态、Release 页面和 `package.zip` 附件。
