# 更新日志

本文件记录“行舟 · 个人项目与事务中心”的重要变更。主页与默认更新日志使用中文；英文版本见 [CHANGELOG.en.md](CHANGELOG.en.md)。

## 未发布

### 新增

- “收件箱”从占位页升级为真实功能：支持仅填写名称并按 Enter 快速捕获。
- 新增条目直接保存为目标属性视图中的独立行，并利用原生“快速收件箱”视图自动填入“收件箱”状态。
- 新增收件箱列表、成功与错误反馈、写入后复核，以及跳转到“全部”详情和原始数据库的入口。
- 新增详情编辑，可直接保存名称、类型、状态、计划日期、截止日期、预计时长、所需精力和行动字段，并在写入后重新读取复核。
- 新增“已计划”状态语义：设置计划日期时可从“收件箱／待开始”转为“已计划”，但不会根据日期自动变成“进行中”。
- 新增根据本地日期动态计算的“今日”和“已逾期”标记；标记不回写数据库状态。
- “本次行动细则”已接入完整读写能力；数据库缺少该字段时继续保持非破坏性兼容提示。
- “本周”升级为真实功能：按实际日期显示周一至周日，并支持上一周、本周与下一周导航。
- 新增“待安排”执行池，只显示尚未设置计划日期的事务与想法；项目和长期领域不会混入行动候选。
- 新增日期窗口提醒：事务或想法处于计划日期与截止日期之间时，会出现在“进行窗口”区域。
- 周视图支持安排日期、移动日期、取消安排和直接完成，并在每次写入后重新读取复核。
- “整理”升级为五步周度检查，覆盖收件箱、活跃顶层项目、遗留日期、行动细则和本周已结束条目。
- 周度整理新增单项优先级：同一条目每轮只出现在一个最先需要处理的步骤中，避免重复决策。

### 调整

- 顶部状态从“只读预览”改为“本地数据库”。
- 状态编辑器精简为“收件箱、待开始、已计划、进行中、阻塞、暂停、将来、已完成、已失败、已取消、已放弃”；旧状态在数据库迁移前仍可兼容读取。
- “全部独立工作项”缩短为“全部”，并在其下直接列出无上层条目。
- 类型与状态移入统一属性区，顶部只保留名称与直接操作。
- 完成按钮移动到名称右侧并与名称框等高；完成后按钮自动消失。
- “本次行动细则”和“下一步行动”改为独立就地编辑：失焦自动保存，`Esc` 取消，`Cmd/Ctrl + Enter` 保存。
- “本周”的待安排列移动到最左侧，并增强事务卡片的边框、底色和阴影层次。
- “整理”页增强卡片分隔、主题色强调线和响应式布局，同时继续跟随思源主题与暗色模式。

### 修复

- 修复从收件箱查看详情时左栏未精确选中条目、中间层级列表仍可能为空的问题。
- 修复数据库块 ID 为空时未回退到默认目标块的问题。
- 允许选择和复制详情区域文字，并明确数据库独立条目不需要关联文档。
- 修复行动编辑按钮与内容区域距离过远、编辑模式造成控件位移的问题。
- 修复整理页单个条目占半行时暴露空白网格底色的问题。
- 修复整理页左侧强调色在部分主题缺少警告色变量时失效的问题。
- 修复同一条目同时缺少行动细则和逾期时在周度整理中重复出现的问题。

### 注意

- 本次提交不自动迁移数据库字段、状态选项或原生视图筛选条件；这些结构变更需要单独执行和复核。

## 0.1.0 - 2026-08-29

### 新增

- 建立思源插件工程和正式安装包构建流程。
- 以原生属性视图为唯一数据源，读取“全部工作项”视图。
- 新增长期领域、独立工作项、层级树和详情三栏界面。
- 新增全部、活跃项目、将来／也许、已结束筛选。
- 新增“仅展开活跃路径”和“全部收起”。
- 展示本次行动细则、下一步行动、计划日期、截止日期、预计时长和所需精力。
- 支持打开关联文档和原始数据库块。
- 新增层级关系检查，但不自动修正用户数据。
- 新增思源主题变量、深色模式和窄窗口适配。
- 新增属性视图 ID 与数据库块 ID 设置。
- 新增中英文 README 与 CHANGELOG。
- 使用“一叶扁舟、动态星轨与渐变水面”作为正式插件图标，并提供随思源主题变色的顶部工具栏图标。
- 主导航调整为“全部、本周、收件箱、整理”，并将“全部”放在首位。
- 工作区改为画布与独立面板结构，增强侧栏、层级树和详情区的视觉分隔，同时继续跟随思源主题。

### 修复

- 修复自定义页签打开 ID 与注册规则不一致导致的空白页。
- 放宽挂载容器判断并补充挂载测试，提高不同思源环境下的兼容性。
- 界面启动失败时改为显示可读错误信息，避免再次出现无提示空白页。

### 限制

- 除收件箱快速新增独立行外，当前不修改属性视图字段或其他单元格。
- 本周和整理页尚未接入实际功能。
- “本次行动细则”字段缺失时只显示兼容提示，不会自动创建。
