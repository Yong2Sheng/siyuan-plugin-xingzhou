# 更新日志

本文件记录“行舟 · 个人行动与生活系统”的重要变更。主页与默认更新日志使用中文；英文版本见 [CHANGELOG.en.md](CHANGELOG.en.md)。

## 未发布

### 新增

- 层级浏览（“全部”页）筛选组新增**「今日」视图**（置首，角标显示今天仍待办的事务数）：只看今天仍有未完成执行切片的事务，其余一律不出现，便于只盯今天的事；命中事务保留完整的长期领域→项目→任务→事务路径作为上下文，但只有事务本身带蓝色“今日”标记；左栏范围分组、工具栏开关与展开/收起一并跟随该筛选。此前列表里只有行内“今日”标记、没有对应的筛选入口，现在两者共用同一套判定（`isTodayFocusItem`），不会出现“标记说今日、筛选说不是”的矛盾。
- “今日”视图支持随视图状态一起记住：重启插件后仍停在离开时的筛选（`ui-state.json` 的筛选白名单已同步扩展）。

### 调整

- **“今日”下取消当日切片后不再跳到别的事务**：此前在详情页点掉今天的格子，事务立刻掉出筛选，界面会按“今日未完成切片最多 → 树序第一”把你切到另一个事务上——而切片日历换事务时会回到本月，看起来几乎没变，很容易以为操作没生效而反复点击。现在改为把选中项**钉在视图里**：它连同上层路径继续显示并带“已移出今日”标记（悬停说明原因），详情面板与日历都不换人，可以直接把切片重新安排回今天恢复；只有主动点别的条目、切筛选、切范围或删除条目时才放下。删除后仍按原规则回落到其他可见事务。
- 执行切片卡片的**信息区压掉一整行**：卡片够宽（≥620px）时，“安排说明 + 完成/放弃本次切片”并入“0% 事务完成度 + 进度条”那一行；较窄时排在进度条下一行，但按钮始终与说明同排，不再因为宽度或状态变化换行、跳动。按钮高度由 30px 收到 22px（3px 内边距 + 14px 行高），说明过长先省略、悬停看全文。
- 切片卡片文案改为陈述事实：今天有已安排切片时只写**“今天已安排 N 片”**，删除原先的“今天结束后未处理会自动记为‘未完成’”——那句话既没有说清是事务还是切片，时点也不准确（实际是下次加载时才结算过去日期的切片，且只改切片状态、不动事务）。
- 层级浏览里选中项被保留时不再插入整条说明横幅，也不再改面板副标题：横幅出现/消失本身会让层级视图跳动，说明改由“已移出今日”标记的悬停提示承担。

### 修复

- 修复按钮“跳到下一行”的问题：说明与按钮此前按容器宽度在“并排/上下”之间切换，调整窗口宽度时按钮会跳位；现在同一档位内不切换布局。

## 2.1.0 - 2026-09-11

### 新增

- **目标切片做满即完成事务**：完成最后一片时事务自动同步为“已完成”——本周的“完成／补记完成／提前完成”、事务详情页的执行切片日历与今日操作、生活节律“今日个人安排”里的完成操作走同一条规则，不再需要回到详情页再点一次“完成事务”；“撤销完成”使目标不再做满时，事务自动退回“进行中”。已结束（已完成／已失败／已取消／已放弃）的事务不会被切片动作改写状态。
- “生活节律 · 今日个人安排”每行新增**取消安排**：只撤掉今天这条切片，事务本身状态不变；撤掉后该行即时消失，并同步更新每日记录里的“今日个人安排”快照。
- “昨晚熄灯”新增**就寝时段**选择（12 点前／12 点后（熬夜）），并显示在标签行、与标签同排垂直居中：
  - 12 点后只需一次点击即可记录，**不必编造具体分钟**：时间行变成“不记具体时间”，想记时点“填时间”再展开；
  - 填了具体时间就由时间自动判定时段（凌晨时间即 12 点后），下拉里与时间冲突的时段置灰不可选，避免出现自相矛盾的数据；
  - 想清掉已填时间只能自己点“清空”，插件不会替你删数据；清空后保留时段标记，回到“不记具体时间”；
  - 历史列表的睡眠时长后显示“熬夜”标记；关键字段检查中“12 点后”标记即视为已填写，不再反复提示补时间。
- 熄灯时段与具体时间一并保存：旧记录没有该字段时按已有时间自动补出，读写、修订号、轮换备份与写后复核流程均不变。

### 调整

- “今日个人安排”的行开始显示事务自身的结束状态：事务已完成／已失败／已取消／已放弃时**保留该行**并标出“事务已完成／事务已取消”徽标，完成、放弃与取消安排一并停用，不再出现“事务已取消、今天这条却仍显示待执行并可操作”的矛盾状态。
- “今日个人安排”的框高由固定 76px 改为**自适应**：最少约三行，超过约五行后转为框内滚动；空状态与有内容时同高，新增或撤掉安排时整段不再跳动。

### 修复

- 修复切片动作绕过图片清理登记的问题：切片把事务自动推进到终态时，与“标记为完成”一致地登记待清理图片；撤销后事务退回“进行中”时清除该登记。
- 修复每日记录表单控件**比自己的栅格列宽出 22px** 的长期问题：控件此前按 content-box 计算，`width:100%` 之外还会再加上左右内边距与边框。1440px 窗口下“今天最重要的工作内容”会与右列“调整内容”相交 12×94px，1100px 窗口下“手表睡眠评分”会顶进邻列。现在所有输入框、下拉与文本域统一按 `border-box` 计算，控件外框与列边界对齐。
- 修复“昨晚熄灯”标签被下拉框顶低 3px、与同一行其它字段标签错位的问题。

- **更正（后续版本）**：上面“目标切片做满即完成事务”的做法已在后续版本改回**由你确认**——目标切片做满不再自动结束事务，只在切片日历上提示“目标切片已全部完成，事务是否也已完成？”，点“完成事务”才改状态；详情页切片日历同时补上**撤销完成**入口（点已完成的格子或右键菜单，今天退回“已安排”、过去日期退回“未完成”，事务若已被手动标记为“已完成”会随之退回“进行中”）。当时实现的撤销侧规则（不再满额则退回“进行中”）沿用至今。详见本文件顶部的“未发布”小节。

## 2.0.0 - 2026-09-10

### 新增

- “本次行动细则”支持图片：直接粘贴截图（⌘/Ctrl+V）或把图片文件拖到卡片上即可插入；图片按**原分辨率**存入思源资源库，不重编码、不压缩，插入后细则里只保留 `assets/…` 路径引用，图片字节不进入插件数据。
- 图片按内容指纹命名并自动去重：同一张图片多次粘贴只存一份；思源内核本身也会按内容复用已有资源。
- 编辑态在输入框下方显示缩略图：上传中转圈、失败标红、带体积角标、可单张移除；阅读态由 Lute 渲染图片，限高显示，点击可看原图。
- 条目结束（已完成／已失败／已取消／已放弃）时自动登记待清理图片：把该条目与所有下级引用的图片记入待清理队列，进入 7 天宽限期；宽限期内细则与图片原样保留，条目上显示“图片待清理 · 还剩 N 天”角标。
- 新增“图片清理”页面（工具栏入口，带待清理条数的数字角标）：
  - 列出所有待清理条目，显示缩略图、张数、状态与倒计时，到期的排在前面并高亮；
  - 点“现在清理”进入页内确认步骤，逐条、逐张勾选范围；被其它未结束条目引用的图片标明引用方并禁止勾选；
  - 确认后先摘掉行舟的图片引用，再逐张核对思源“未引用资源”列表，只删除思源确认无人引用的文件；被文档、数据库或其它插件引用的图片一律保留；
  - 删除通过思源单张删除接口完成，思源会先把文件复制进历史目录，可在「历史」中找回；清理结果在页面内展示（删除数、保留数、失败原因）。
- 新增“图库体检”区块：统计行舟引用的去重图片数、未结束条目占用、待清理占用与资源库总量，并给出分段占用条与按条目占用排行（折叠，最多 20 条）。体积通过思源资源接口逐个查询，结果缓存，可手动重新统计。

### 调整

- 图片清理入口从“工具栏下方常驻面板”改为“工具栏右侧按钮 + 独立页面”：不再占据纵向空间，也不再受弹窗宽度限制。
- 清理确认由弹窗改为页面内第二步，勾选框独立放在缩略图上方，文案固定为“删除／不可删除”；点缩略图本身也能切换是否删除。
- 缩略图按原始比例完整显示（不再裁切或被拉伸），长截图也能看清内容。
- 视图状态新增“清理”页记录，重新打开行舟会回到上次所在的清理页。

### 修复

- 修复新增字段未同步到新建对象导致的**所有写入被“完整性复核”拦下**：`addStoredWorkItem` 现在显式初始化 `imageCleanup`，写入与重新读取的结果保持一致；复核失败时错误信息会指出首个差异位置，便于定位。
- 修复多处图片交互缺陷：
  - 多张图片连续粘贴时，后一张会插进前一张语法中间把图片切碎（光标偏移方向算反）；
  - 连续图片之间会多出空行；
  - Svelte 依赖追踪失效导致删除缩略图后列表不刷新（依赖写在 `map` 回调内不被追踪）；
  - 上传未完成时失焦保存会写入半成品路径，现在不会写入并会移除未完成占位符；
  - 上传未完成时切换条目，迟到的上传结果会写进新条目（已加草稿归属与过期判断）；
  - 占位符正则未覆盖“带文件名”的形式，导致上传成功后替换不回去；
  - 勾选后取消无法再勾上（勾选状态原先从派生值反推），现改为独立维护；
  - “不可删除”误标：可删除与否曾与勾选状态耦合，现只由“是否被其它未结束条目引用”决定。

## 1.3.0 - 2026-09-09

### 新增

- 执行切片日历与“本周”页签新增“待做”时长：当天状态仍为“已安排”（尚未完成）的切片时长合计。当天承诺的事全部做完或提前做完时，格子显示绿色的“待做 0 分”并把整格标绿，一眼就能看出这天已经空出来、可以再安排其他事务；旁边保留原有总量的“共 X 分 · N 片”（已安排＋已完成），两个数字一对上就不会把“待做”误读成空闲时间。
- 执行切片日历头部新增一行汇总与口径图例（今天起的待做合计、总量合计、已清空的整天数），说明“待做＝当天已安排但还没完成的切片时长”。
- 重新打开行舟时会恢复上次离开“项目与事务”时的视图状态（页签、筛选、层级展开、选中事务与滚动位置）；上次选中项已完成或不可见时，按“今日未完成切片最多（并列取树序第一）→ 树序第一个可见事务”确定性回落到可见事务。
- 生活节律早晨“今日安排”新增只读提示：前一天 21:00 复盘填写的“明天开始工作时的第一个动作”会在次日早晨自动显示，仅供早上参考；提示不可编辑，前一天未填写或未记录时不显示。
- 生活节律“个人事务补充说明”新增“是／否”前置判断：只有选择“是”才展开输入框，选择“否”立即收起且不占高度；科研工作日／周日半日、周六、休假与开会日“会后”四处统一生效。

### 调整

- 执行切片日历的日期格子改为两行时间块：第一行“待做 X 分”，第二行“共 X 分 · N 片”；过去日期只显示“共”（那天的待做已经没有意义，未完成的切片仍由红点、红边与“未完成”表示）；没有预计时长的切片显示“待做 未估时”，部分估时时显示“待做 ≥X 分”。
- 日期格子按容器宽度自动收敛：宽格子写“待做 240 分”，中等宽度去掉数字两侧空格写“待做240分”，最窄（约 41px）时主行让出“分”并把单位留在“共240分”那一行，任何宽度都不会只剩一个裸数字。
- “本周”页签的日期行改成三个固定槽位：左侧“周X＋日期”、中间固定给“今天”（其它日期留空）、右侧为“待做”chip 并始终贴右，不再出现“今天在右、chip 在中间”的跳位；列宽不足时 chip 省略“分”，窗口拉宽后自动恢复。
- 移除执行切片日历格子与“本周”待做 chip 的鼠标悬浮提示（读屏说明保留在无障碍标签中），并删除“本事务 N 片 · 完成 M”这行永远不会出现的冗余文案：同一事务同一天只允许一个切片，创建与移动都会拦截。
- 层级浏览的配色系统重塑：类型徽章采用沉稳的莫兰迪低饱和色板（灰卡其／灰绿／灰青／灰蓝／陶土／灰紫），状态与切片指示采用高饱和夏日色板（进行中=亮绿、待开始=亮天蓝、重点投入=亮橙、暂停=亮黄、将来=松石青、持续维持=亮靛蓝、阻塞/已失败=亮红、待安排=亮琥珀、已安排/今日=深蓝、终止类=灰），并统一为“方片小圆角＋彩色描边＋左侧 3px 彩条”样式；形状与色彩气质双重区分，类型与状态不再混淆。
- 生活节律顶部工具栏改为左对齐：日期导航与六个视图按钮连排靠左，右侧留白，减少大屏下的切换移动距离；窄屏仍按纵向堆叠。
- 事务详情“执行切片”卡片压缩纵向占用：头部合并为一行，说明文字移入标题旁 ⓘ 悬停提示；“完成百分比”与进度条、排期摘要同行，小屏下不再浪费空间。
- “目标切片数”标签后标明输入上限 366，便于了解单个事务最多可按年保留 366 个切片。
- 个人事务区改为“左列判断＋输入框、右列今日个人安排”：补充说明的判断下拉与它展开的输入框上下紧邻，不再一个占整行、一个落在右下角；选择“否”时正文清空但内容暂存，改回“是”即可恢复；旧记录里已有的补充说明会自动识别为“是”，不会被隐藏。

## 1.2.0 - 2026-09-08

### 新增

- 新增“开会日”日期类型，适用于 conference、talk 和合作会议：会议开始至结束不预设自由时间，会后再选择是否安排个人事务，并明确跳过当日营养摄入记录。
- 每日 Checklist 新增“部分完成”和“未完成”，与“待处理”“已完成”组成四态结果；部分完成按半项折算进度，旧版勾选会继续识别为已完成。
- “项目与事务”新增自动生成的“关系图”，默认展示全部未完成项目与事务及其长期领域上下文，并同时呈现上下层、“完成后开始”和“需先行”关系。
- 层级浏览中的事务新增切片状态提示，可直接区分“未设切片”“待安排 N”“已安排 X/Y”和“切片完成 X/Y”。
- 事务日历新增右键完成菜单：未来切片可提前完成、今日切片可完成、过期切片可补记完成；也支持键盘菜单键与 Shift+F10。
- 手表睡眠评分和晨起体重新增“是／否”前置选择，没有佩戴手表或没有体重计时可明确标记为无需填写。

### 调整

- “今日”标签现在只显示在当天仍有未完成执行切片的事务上；完成当天切片后立即消失，不再向长期领域或项目祖先汇总。进行中、重点投入和今日分别使用绿色、红色和蓝色提示。
- 依赖选择器只列出“进行中”和“待开始”的候选事务，减少已结束或暂不相关工作项造成的长列表。
- 提前完成未来切片时保留原计划日期，以维持同一事务每天只安排一个切片的语义；本周视图同时在实际完成日显示一条不计入当日安排的提前完成成就记录。
- 关系图采用更明确的方向文案，例如“前项完成 → 后项开始”和“前项先行 → 后项”，并增加卡片间距、相关节点高亮、点击空白处取消高亮以及更易读的右侧详情栏。
- 科研工作日“今日安排”重新排版：上班和计划下班时间同列同行，临时调整及其条件输入保持在右侧对应位置。

### 修复

- 修复关系图有数据但画布空白的问题，并修正多个依赖箭头重叠、关系方向不清和状态标签样式偏离设计的问题。
- 修复本周视图“提前完成”无响应或完成后卡片消失的问题；原计划日现在保留完成卡片，实际完成日另显示成就提示。
- 修复同一事务同一天存在多个历史切片时，事务日历只显示一个切片却仍报告错误完成数量的问题；日历现在显示该事务当日总切片数和完成数。
- 修复生活节律阶段状态未及时同步，以及条件输入出现后今日安排左右布局错位的问题。

### 兼容性

- 本版本不要求手动迁移工作项、执行切片、生活节律、Checklist 或营养数据，也不会改写已有切片的计划日期。
- 旧版 Checklist 的 `checkedKeys` 会在读取时转换为“已完成”；旧版生活节律记录会根据已有评分和体重值推断对应的“是／否”状态。

## 1.1.0 - 2026-09-06

### 调整

- 午饭后专业学习改为先确认“没有／有”；仅在有安排时显示书目、主题、计划和完成记录，明确没有时清空不再适用的旧内容。
- “生活节律”今日记录新增阶段完整度与待补检查：保留自由跳跃填写，阶段按钮显示未开始、待补、完成或无需检查，待补清单可直接跳转并聚焦关键字段；普通备注仍保持可选。
- 营养管理区将“每日目标”和“常用模板”拆为两个独立卡片，宽屏并列、平板与窄屏上下排列，明确区分目标设置与食物模板录入。
- 移除重复的右上角全局添加按钮、收件箱导航与独立页面，并从“整理”中移除收件箱步骤；新增统一通过已分类入口完成且默认进入“待开始”，旧收件箱状态数据继续在“全部”中兼容显示。
- 整理页将投入检查拆分为“重点投入的长期领域”和“进行中的顶层项目”，分别呈现近期重视什么与正在做什么，避免“投入方向”术语混淆。
- Checklist 的每日勾选进度和周末训练／休息安排改为按日期持久保存，重启插件或切换同步设备后不再清空；现有模板配置无需迁移。
- “当日已吃”改为独立纵向滚动区，标题保持可见；桌面端根据窗口高度充分利用下方空间，平板单列采用更紧凑的高度上限，记录较多时不再无限撑长页面。
- 每周视图恢复执行切片的完成入口：今天可完成或放弃，过去的未完成切片可补记完成，未来切片可提前完成并自动移到今天，避免产生未来日期已经完成的矛盾记录。

## 1.0.0 - 2026-09-06

### 新增

- “生活节律”新增独立的“营养摄入”页面，按日汇总热量、蛋白质、碳水和脂肪，不要求区分早餐、午餐和晚餐。
- 支持设置每日热量与蛋白质目标，并显示进度、剩余额度或超出量；碳水和脂肪作为辅助指标，同时显示按 4／4／9 kcal 每克近似换算的宏量营养热量占比。
- 支持自建、编辑和删除常用食物模板。模板可一键按基准量记录，也可临时输入实际重量、容量或数量，四项营养数据会按比例自动换算。
- 当日记录可直接修改实际摄入量，并按单位提供适合的增减步长；修改或删除模板不会改写历史记录中的营养快照。
- 营养数据使用独立的 `nutrition.json`，具有修订号、三份轮换备份、写后复核和损坏恢复保护。
- 所有执行切片日历新增每日已安排切片数和预计分钟数，帮助在排期前识别工作负荷；紧凑空间优先保留预计时间。

### 调整

- 重构插件顶部布局，保留“项目与事务／生活节律”原有按钮样式，同时压缩无信息空白，不改变下方既有业务界面。
- 执行切片顶部将总投入、状态图例、当日提示和操作整合到紧凑信息区，并缩短配置项与日历之间的距离。
- 针对平板和窄列重新设计响应式布局：详情字段在空间不足时稳定换行，层级浏览减少无效占位，营养页面按插件实际容器宽度切换双列、单列和底部按量弹层。
- 增强触控操作目标，同时保留键盘、触控板、右键菜单和桌面密度。
- 执行切片完成后可自动把尚未开始的事务推进为“进行中”，不会覆盖暂停、阻塞或结束状态。

### 修复

- 提高执行切片日历文字、日期边框和负荷信息的对比度，修复平板上日历被右侧说明严重挤压的问题。
- 修复鸿蒙平板详情字段被压缩、截止日期与所需精力错位，以及层级标题只能显示一两个字的问题。
- 恢复事务详情标题靠左对齐，并保证窄列中的更多菜单和触控操作可用。
- 修复已有执行记录时无法清除截止日期的问题；只有把新的具体截止日期设到已有切片之前时才会阻止保存。

### 兼容性

- 升级后会首次建立 `nutrition.json`，不会读取或修改任何思源文档来生成营养数据。
- 营养模板和记录支持旧版“份数／每份说明”结构，并会在读取时转换为等价的基准量、单位和实际摄入量。

## 0.8.0 - 2026-09-05

### 新增

- 生活节律新增独立的“每日 Checklist”页面，内置与纸质版一致的工作日、周六和周日提醒内容，并移除原纸质表中需要手写填写的空白字段。
- Checklist 同时提供“行舟视图”和“纸质视图”；两者共享模板与当日勾选状态，显示偏好会自动保存，打印时始终使用纸质排版。
- Checklist 的时间节点、标题、提醒内容和纸质区块样式均可编辑，也可新增、删除和调整时间节点顺序；配置由独立文件保存并提供三份轮换备份和写后复核。
- Checklist 勾选只在当次使用期间按日期临时保留，不进入历史数据；切换视图或模块不会丢失，新日期会自动清空旧状态。
- 睡前安排新增“自由安排，不记录计划”，可主动跳过睡前准备和计划熄灯记录，同时保留次日填写实际睡眠数据的能力。
- 计划熄灯新增“当晚／次日”日期选择，并保存完整日期时间，明确表达午夜之后的熄灯计划。

### 调整

- 21:00 复盘中的下班后工作和异常观察改为“尚未确认／否／是”；只有选择“是”才显示说明输入框，选择“否”会清除不再适用的旧说明。
- 工作闭环的下一步安排改为“尚未确认／没有／有”；只有选择“有”才显示下一步内容。
- 重新整理 21:00 复盘和睡前准备的双列布局，使成对字段、条件输入以及熄灯日期和时间保持稳定对齐。
- 生活节律导航将“每日 Checklist”置于“今日记录”之前，形成先行动提醒、后结果记录的阅读顺序。
- 纸质视图改为与行舟视图一致的主清单＋右侧进度布局，并适度放大两种视图的标题、时间、提醒正文和复选框；打印时仍只输出纸质清单。

### 修复

- 修复 Checklist 初次写入后因 JSON 字段顺序不同而被完整性复核误判为保存失败的问题。
- 修复切换到纸质视图时清单突然居中、右侧进度卡消失所造成的布局割裂。

### 兼容性

- 旧记录中已有的下班后工作原因、异常观察和下一步内容会自动推断为“是／有”；“没有／无”等占位文字会归一为明确的否定状态。
- 旧的计划熄灯时间会按夜间语义补全日期：午夜前归入当晚，午夜后归入次日。

## 0.7.0 - 2026-09-04

### 新增

- 为事务新增“执行切片”：可设置目标切片数与每片预计时长，在详情页日历中独立安排日期；切片不是子事务，也不参与上下层关系。
- 新增已安排、已完成、未完成和已放弃四种切片状态。事务完成度按已完成切片占目标数的百分比显示；未完成或放弃保留历史，同时释放一个新的待安排名额。
- “本周”按执行切片呈现每日安排，并支持完成与撤销；配置切片的事务不再按计划开始日至截止日期机械生成每日副本。
- 生活节律自动显示当天的个人事务切片，也可从进行中的事务补充当天切片；改动会立即同步回“项目与事务”。
- 无截止日期的事务也可以从今天起安排或移动执行切片；存在截止日期时仍以它作为最晚可安排日期。

### 调整

- 每日记录改为延迟自动保存；切换日期、填写阶段或一级模块前会先完成待保存内容，保存失败时停留在当前页面并允许重试。
- 科研工作日拆分为“早晨／午饭后／下班／下班后／21:00”五个阶段。“下班”负责工作边界与状态评估，“下班后”负责工作闭环和个人事务。
- 训练完成状态移至早晨并置于训练内容之前；只有选择“已完成”时才显示训练内容，选择“未完成”或“休息日”会清除不再适用的内容。
- 从生活节律返回“项目与事务”时恢复离开前的页面、筛选、层级范围与展开状态、选中工作项和滚动位置；从每日事务跳转时仍可精确聚焦目标。
- 生活节律中的时间、评分、体重、个人事务和结果字段采用更稳定的响应式宽度及对齐方式。

### 修复

- 修复切片数量保存后日历仍不可点击，以及安排切片时日历和详情控件短暂跳动的问题。
- 修复“关键工作结果”等受控选择框首次选择未立即显示的问题，并消除评分清除按钮造成的布局位移。
- 修复今日个人事务选择器与层级浏览排序不一致、切换模块后错误聚焦工作项或缩小到单个长期领域的问题。
- 修复无截止日期事务无法使用执行切片日历的问题。

## 0.6.0 - 2026-09-03

### 新增

- 层级浏览的每一行提供上移、下移按钮，也支持拖动手柄调整同级工作项顺序；顺序保存在插件内部数据中，刷新后保持不变。
- “本周”中的完成操作改为逐日记录：完成某一天不会再结束整个工作项或影响其他日期；已完成日期保留在看板中并支持撤销，工作项整体完成仍在详情页操作。
- 新增与“项目与事务”并列的“生活节律”一级模块；原有“全部／本周／收件箱／整理”保持为项目与事务的二级视图。
- 新增今日记录、历史数据、评分标准和时间线，并支持科研工作日、周六轻量复盘、周日半日科研与休假／节假日四种日期类型。
- 新增完整的分阶段每日表单、小时与分钟下拉输入、kg/lb 体重单位、工作边界自动计算，以及填写时可见的五套评分规则。
- 新增下班后工作闭环的“尚未确认／不需要／需要”三态；仅在需要时显示详细字段，不需要时将相关时长保存为空值而非 0。
- 新增独立的 `daily-records.json`、三份轮换备份和写后复核；从空仓库开始，不迁移任何旧每日文档。
- 为后续周／月统计及 AI 分析提供按日期范围读取每日记录和读取评分规则的只读接口。
- “本周”按计划开始日到截止日期的完整区间展示工作项，并区分“开始”“持续中”“截止”“承接上周”和“延续下周”；周统计仍按工作项去重，预计时长只在开始日显示。跨日工作项仅可从首张卡片修改开始日，并阻止开始日晚于截止日期。
- 层级浏览为今天开始、今天截止或执行区间覆盖今天的工作项显示“今日”，并把数量汇总到上层项目路径；原有生命周期状态仍保留但弱化显示。
- “本次行动细则”和“下一步行动”在非编辑状态下使用思源 Lute 直接渲染基础 Markdown，支持标题、强调、列表、引用、代码、链接、表格与任务列表等常用格式，并保留普通文本换行；编辑与存储仍保留原始 Markdown 文本。
- 行动内容编辑框支持智能续写编号列表、项目符号列表和任务列表；在空列表项再次回车可退出列表。
- 行动内容编辑框会在进入编辑态时按完整内容高度自动展开，并在内容或窗口宽度变化后继续自适应，避免阅读态与编辑态之间突然缩小。

### 修复

- 调整生活节律表单的字号、响应式宽度与列对齐，避免宽窗口下时间、评分和体重控件不协调拉伸。
- “昨晚熄灯”与“今日起床”现在保存为带日期的明确时间，正确表达跨日睡眠；同时修正实际下班时间、睡眠评分和评分标准入口的位置。
- 修复关键工作结果首次选择后仍显示“尚未填写”的问题，并允许再次点击已选评分直接清除，避免动态按钮引起布局跳动。
- 修复 Markdown 阅读态错误显示思源内部块 ID 属性标记的问题。
- 修复在编号列表中间插入或删除项目时，编辑态编号与阅读态不一致的问题；输入、删除、剪切和粘贴后会立即校正后续同级编号。

## 0.5.0 - 2026-09-02

### 新增

- 工作项数据改由插件内部完整保存，不再依赖额外的思源属性视图或文档；旧属性视图只在首次启动时作为一次性迁移来源。
- 首次迁移保留名称、类型、状态、行动内容、日期、精力、文档关联、层级关系与跨项目依赖，并保存内部迁移快照。
- 每次写入前轮换保留三个内部历史版本，写入后立即重新读取并逐项比对；主数据损坏时优先从最新可用备份恢复。
- 新增独立于层级树的跨项目依赖：“完成后开始”和“需先行”可连接任意工作项，关系由插件直接保存，无需新增数据库字段。
- 详情页支持添加、移除及反向查看依赖，层级树显示依赖数量；硬依赖未完成时给出提示，但不自动改写工作项状态。
- 写入前阻止自依赖、同一前置项重复归入两种强度以及跨强度循环依赖；删除被引用工作项前补充风险提示。

### 调整

- 新增、编辑、排期、完成和删除均只修改插件内部数据。迁移成功后，即使旧数据库或承载它的文档被删除，行舟工作项仍会保留；已删除的关联文档本身仍无法再打开。
- 删除工作项不会级联删除下级，但会自动清除其他工作项对它的父项、顶层项目和依赖引用。

### 修复

- 正确识别思源在插件数据文件尚不存在时返回的空对象，避免首次迁移被误判为数据损坏。

## 0.4.0 - 2026-09-01

### 调整

- 从可选执行状态中移除“已计划”，并把界面中的“计划日期”明确改称“计划开始日”。
- 未来开始的事项显示弱提示“已安排”；到达开始日时，“待开始”及旧“已计划”事项自动进入“进行中”，同时保留阻塞、暂停和结束状态。
- 旧数据库中的“已计划”仍可读取，并会按计划开始日自动归一为“待开始”或“进行中”。
- 整理页不再把已经过去的计划开始日视为遗留日期，避免正常的多日进行中事项被反复提醒。
- “收件箱”改为纯粹的整理入口，不再出现在项目或执行状态选项中；上下文新增和完成分类的条目默认进入“待开始”。
- 新增层级状态向上推进：任意进行中的后代会把仍在“收件箱／待开始”的项目祖先推进为“进行中”，但不会覆盖长期领域或明确的暂停、阻塞、将来和结束状态，也不会自动倒退。

### 修复

- 修复已经安排到当前周日期列的执行事项仍重复出现在左侧“进行窗口”的问题；无计划日期条目和跨周有效窗口提醒不受影响。
- 修复侧栏卡片空间不足时“已完成”等状态被压缩成竖排文字的问题。

### 新增

- 在顶部筛选区新增“包含已结束”开关；“全部”默认隐藏结束条目并记住本机选择，“已结束”筛选仍始终展示完整结果。
- 隐藏结束条目时保留承载未结束后代所必需的结束上层，避免层级路径断裂。

## 0.3.0 - 2026-08-31

这一版本把新增工作项、层级识别和完成操作整理成更贴近思源原生体验的工作流。

### 新增

- 页头新增全局“＋ 添加”入口，在全部、本周、收件箱和整理页都能以名称快速写入收件箱，并支持 `Cmd/Ctrl + Shift + I`。
- “全部”页可从长期领域或项目详情及右键菜单添加下级工作项，自动带入直接父项与可推导的顶层项目。
- 三个侧栏分区新增紧凑的 `＋`：可直接添加长期领域／想法、可选所属领域的顶层项目，以及独立事务。

### 调整

- “全部”页侧栏移除重复的总入口，改按“长期领域与想法、顶层项目、独立事务”分区，并在需要时显示“待归类”。
- “全部”筛选默认完整展开层级；“活跃项目”改为显示并展开活跃项目路径中的执行事项。
- 新增工作项窗口改用思源原生 `Dialog`，移除高 GPU 占用的实时背景模糊，并沿用思源原生表单、按钮、主题与窗口行为。
- “添加顶层项目”不再显示只能选择“项目”的下拉框；窗口明确区分“顶层项目”层级角色与数据库中的“项目”类型。
- 层级树改用带文字与主题色的角色标签，移除重复圆点；层级图例、中间树与右侧详情统一显示长期领域、顶层项目、子项目、任务、事务和想法。
- 完成快捷操作移至角色标签行，并改名为“标记为完成”和柔和绿色样式，避免被误认为名称编辑的确认按钮；完成后提供 8 秒撤销入口。

### 修复

- 修复在当前长期领域或项目范围内新增下级后，左侧范围被强制切换到直接父项目的问题；现在会保持原视图并选中新条目。

## 0.2.0 - 2026-08-30

详情页现在会根据工作项角色只呈现真正适用的信息，并明确区分尚未确认、明确无期限和已有日期三种截止日期状态。

### 调整

- 右键工作项菜单改用思源原生 `Menu` 组件，与思源任务中心保持一致的图标、危险项、悬停效果、主题和暗黑模式表现。
- 详情页改为角色感知布局：长期领域、顶层项目、子项目、任务、事务和想法只显示真正适用的字段。
- “所属顶层项目”由直接上层链推导并只读显示；修改直接上层时同步写入推导结果。

### 新增

- 新增“无截止日期”复选框支持，将截止日期区分为“待确认”“无”和“具体日期”；整理页会提醒未确认，但跳过明确无期限的条目。

## 0.1.1 - 2026-08-30

在首个正式测试版本上补充受保护的工作项删除流程。

### 新增

- 在全部页、收件箱、本周、整理等工作项入口提供统一右键菜单，可删除长期领域、项目、任务、事务和想法。
- 删除前提供二次确认，并在目标仍有下级工作项或“所属顶层项目”引用时显示关系警告。

### 安全性

- 删除操作只移除目标属性视图条目；已绑定的思源文档仅解除绑定，不会被删除。
- 删除不会级联处理下级工作项，也不会静默改写其他条目的关系；操作后会重新读取属性视图复核结果。

## 0.1.0 - 2026-08-30

首个可正式使用和持续测试的版本。发布前按方案报告逐项审计，首版核心需求覆盖度约为 94%。

### 新增

- 建立思源插件工程、正式安装包和中英文说明体系，并使用“一叶扁舟、动态星轨与渐变水面”作为视觉标识。
- 以原生属性视图为唯一数据源，提供“全部”“本周”“收件箱”“整理”四个一级页面。
- 在“全部”页提供长期领域、项目层级、独立工作项、活跃路径展开和全部收起，以及全部、活跃项目、将来、已结束筛选。
- 支持直接编辑名称、工作项类型、状态、上层工作项、所属顶层项目、计划日期、截止日期、预计时长和所需精力，并在写入后重新读取复核。
- 支持“本次行动细则”和“下一步行动”独立就地编辑：失焦保存、`Esc` 取消、`Cmd/Ctrl + Enter` 保存。
- 新增名称快速捕获、收件箱列表、成功与错误反馈，以及数据库独立条目详情。
- 新增按真实日期组织的本周视图、上一周／本周／下一周导航、待安排执行池、有效日期窗口提醒、移动日期、取消安排和快速完成。
- 新增五步周度整理，覆盖收件箱、活跃顶层项目、行动细则、遗留日期和本周成果；同一条目每轮只出现在最高优先级步骤中。
- 新增“已计划”状态语义和根据本地日期推导的“今日”“已逾期”提示，日期不会在后台篡改实际进度。
- 支持打开关联思源文档、原生数据库入口和非破坏性的层级关系检查。
- 适配思源主题变量、暗黑模式和窄窗口布局，并提供属性视图 ID 与数据库块 ID 设置。

### 调整

- 状态编辑器统一为“收件箱、待开始、已计划、进行中、阻塞、暂停、将来、已完成、已失败、已取消、已放弃”，同时兼容读取旧状态。
- 类型与状态进入统一属性区；名称与完成按钮同一行、等高显示，完成后按钮消失。
- 周视图的“待安排”位于最左侧，并且只收纳无计划日期的事务与想法。
- 本周日期栏只呈现尚未结束的事项；已完成内容由“整理”的本周成果步骤承接。
- 强化全部页、本周卡片和整理卡片的边框、底色、阴影和主题色强调线。
- 主导航和筛选统一使用“全部”“将来”等简洁名称。

### 修复

- 修复自定义页签注册不一致、挂载容器判断过严等导致的无提示空白页。
- 修复从收件箱查看详情时未精确选中条目，以及捕获后层级结果未及时更新的问题。
- 修复旧设置中的空数据库块 ID 未回退默认值的问题。
- 恢复详情文字选择，并明确数据库独立条目不要求关联文档。
- 修复行动编辑控件位移、周度整理空白网格底色、部分主题强调线失效等界面问题。
- 修复同一条目同时存在遗留日期和缺少行动细则时在整理页重复出现的问题。

### 已知限制

- 插件不会自动新增、删除或迁移数据库字段和状态选项。
- 折叠状态与默认页面尚未持久化。
- 暂不提供拖放排期、并发编辑冲突提示、原生数据库记录行精确定位、自动创建文档和层级图挂件。
