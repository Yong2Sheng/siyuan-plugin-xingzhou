# 行舟 · 图片功能开发报告（v2.0.0）

> 这份文档记录「本次行动细则支持图片 → 待清理 → 清理 → 图库体检」这条功能线（v1.3.0 → v2.0.0）开发过程中**真实踩到的每个问题、根因、修法与验证方式**，以及仍存在的风险。
>
> 用途：后续在新对话里继续开发时，先读这份文档，可以避免重复踩坑。文中的结论都经过实测（单测、无头浏览器测量或真实数据核对），不是推测。

---

## 一、整体设计（先看这一节，避免改错方向）

### 数据放哪儿

| 内容 | 位置 | 原因 |
|---|---|---|
| 图片字节 | 思源资源库 `data/assets/xz-<hash>.png` | 只存一份；随思源云同步；可被笔记引用 |
| 图片引用 | 工作项 JSON（`work-items.json`）的 `currentAction` / `nextAction` 文本里的 `![](assets/…)` | 插件数据只记路径，**绝不放 base64** |
| 待清理登记 | 工作项的新字段 `imageCleanup: { startedAt, paths }` | 复用既有持久化的安全机制（版本号、revision、3 槽轮换备份、写后复核） |
| 清理状态 | 无需额外存储 | 条目不再终态即自动失效（`listPendingImageCleanup` 用 `isClosed` 过滤） |

### 绝对不要做的事

1. **不要把图片以 base64/data URI 写进 JSON**。`work-items.json` 有 3 份轮换备份、每次保存写 4 份再叠加云同步，内嵌图片会被放大数倍，这是唯一真正会"爆炸"的做法。
2. **不要自己维护引用计数后直接删文件**。行舟只能看到自己的引用，看不到笔记文档、数据库、其它插件对同一张图的引用。删除前必须查思源的"未引用资源"列表（`/api/asset/getUnusedAssets`），只删里面出现的。
3. **不要用 `position: absolute` 做"固定在右侧"的按钮**。它脱离文档流会直接盖住旁边的筛选器（v1 设计稿踩过）。要用 flex 的独立占位区（`flex: 0 0 auto` + 左侧分隔线）。
4. **不要在弹窗里堆内容**。思源 Dialog 的实际可用宽度不可控，内容过长会出现"右侧一大片空白/文字被切"（本版最终把清理确认改成页内步骤才解决）。

### 关键 API 事实（实测）

- 上传：`POST /api/asset/upload`，字段名是 **`file[]`**（不是 `assets`），另有 `assetsDirPath`。**内核自带内容级去重**：相同字节会复用已有资源路径；但内容相同、文件名不同时会另存一份（`random_2_` 分支），所以插件端仍按内容指纹命名。
- 未引用资源：`POST /api/asset/getUnusedAssets`（**一次最多返回 512 条**），`POST /api/asset/removeUnusedAsset` 单张删除，**删前会复制到思源历史目录**，可找回。
- 体积：`POST /api/asset/statAsset`（单张）。
- 目录列举：`POST /api/file/readDir`，路径基准是 **workspace 根**（`data/assets`，不是 `assets`），而且**不返回文件大小**，体积只能逐个 `statAsset`。
- 图片显示：浏览器/Electron 直接按 `assets/xxx.png` 相对路径请求即可（`GET /assets/xxx.png` 由内核伺服）；**不要把 `assets/` 写成 `data/assets/`**。
- 云同步范围：`data/storage/petal/` 整个插件目录都参与同步；`data/.siyuan/syncignore` 是官方的同步忽略清单（支持 `!` 反选），可用它把行舟图片目录排除出同步。

---

## 二、踩过的坑：Svelte / 前端

### 坑 1：依赖写在 `map` 回调里，Svelte 追踪不到（踩了 3 次）

**症状**：状态变了但界面不刷新。具体表现：
- 从内容里删除缩略图后，列表不更新；
- 取消勾选后，按钮上的"确认删除 N 张"不重算；
- 点击条目后确认步骤的列表不出现。

**根因**：Svelte 4 的 `$:` 只追踪**表达式层级**出现的变量。写成 `$: rows = build(...)`，而 `build()` 里（或 `.map()` 回调里）引用了 `draft`，编译器看不到这个依赖，就不会重算。

**修法两种**：
```js
// ① 把依赖显式提到表达式层级
$: key = `${excludedImages.size}:${excludedItems.size}`;
$: plan = confirming ? (void key, buildPlan()) : [];

// ② 或把纯函数改成显式传参（推荐，本版最终采用）
function buildStorageStats(items, sizes) { ... }
$: stats = buildStorageStats(data?.items ?? [], assetSizes);
```

**判据**：只要"状态变了但界面没变"，先怀疑依赖没被追踪，而不是去改样式。

### 坑 2：勾选状态从派生值反推 → 取消后无法再勾上

**症状**：取消勾选后再点，勾不上了。

**根因**：`<input type="checkbox" checked={image.selected}>` 里 `image.selected` 是派生值。用户手动点掉后 DOM 变 false，但 Svelte 记录的表达式值仍是 `true`；再次点击时它认为"值没变"，于是**不更新 DOM、也不派发 change**。

**修法**：勾选状态**独立维护**（`cleanupSelectedImages` Set，进入确认步骤时初始化一次，之后只由交互改变），删除范围 = 候选图片 ∩ 已勾选。派生值不再反向驱动勾选。

**推广**：任何"用户可改、又由派生计算"的开关，都不要用单向 `checked={derived}`。

### 坑 3：可删除性与勾选状态耦合 → 误标"不可删除"并禁用

**症状**：取消勾选后，该图被标成"不可删除"，复选框禁用，再也点不回来。

**根因**：我把"未勾选的图片"塞进了删除范围 `onlyPaths`，于是 `buildImageCleanupPlan` 认为它"不在范围内 → 不可删"，界面据此禁用了复选框。

**修法**：**正交概念必须解耦**——

| 概念 | 判定来源 |
|---|---|
| 是否能删（复选框能不能点） | 只看 `referencingActiveItems()`：被其它未结束条目引用 → 禁用 + 写明引用方 |
| 是否要删（勾选状态） | 只由交互维护 |
| 实际删除范围 | 候选图片 ∩ 已勾选 |

### 坑 4：`</script>` 字面量会截断页面脚本

**症状**：设计稿页面只渲染出第一个区块，后面全空白；脚本"什么都没执行"。

**根因**：我在预览页的内联 `<script>` 里写了字符串 `"</script>"`（用来剥离 iframe 内的脚本），**HTML 解析器把它当成脚本结束标签**，后面的 JS 全部变成了文本节点。

**修法**：拆开写 `"</" + "script>"`，或用 `<\/script>`。

### 坑 5：批量改 HTML 属性把文件改坏

**症状**：设计稿的"方案 B 区块"里渲染的其实是方案 A 的标记；frame 标签变成 `>>`、`<div>` 未闭合 9 个，DOM 树整体错位。

**教训**：不要用"全局正则替换"去批量改标记的属性/标签；一处错位会连锁破坏全文件。要么逐处精确替换，要么**整份重写**（本版最终重写了预览页）。改完必须校验 `div` 开闭配对与结构。

---

## 三、踩过的坑：数据与持久化（最危险的一类）

### 坑 6：新增字段只加在解析器、漏了新建对象 → 所有写入被复核拦下

**症状**：新建任何条目都报「内部数据写入 `work-items.json` 后未通过完整性复核」，功能等于不可用。

**根因**：写后复核是 `JSON.stringify(写入对象) === JSON.stringify(重新解析的对象)`。我给 `normalizeWorkItem`（解析路径）加了 `imageCleanup: null` 的默认值，却漏了 `addStoredWorkItem`（新建路径）。于是：

```
写: ..."executionSlices":[],"planDate":null...
读: ..."executionSlices":[],"imageCleanup":null,"planDate":null...
```

**修法**：
1. `addStoredWorkItem` 显式初始化 `imageCleanup: null`；
2. 顺带加了 `describeStoreMismatch()`：复核失败时报告**首个差异位置**，下次这类问题一眼可见；
3. 补回归测试：新建/修改/清除三条路径都要"序列化→重新解析"完全一致。

**推广**：给工作项加字段时，必须同时覆盖：解析默认值、新建对象、克隆、比较、以及所有构造 `WorkItem` 的地方。这是 AGENTS.md 明确写过的规则，这次真的踩了。

### 坑 7：同一张图被多条条目引用 → 清理时"0 张可删"

**症状**：确认页显示"0 张"，还配了句驴唇不对马嘴的文案，看起来像程序坏了。

**根因**：用户的库里两条条目引用了同一张图，其中一条还是"待开始"（未结束）。行舟判定"仍被活跃条目引用 → 保留文件"，所以可删数确实是 0——**判定是对的，界面表达错了**。

**修法**：界面必须解释清楚。现在：
- 计数显示「登记 N 张 · 可删 M 张」，而不是只显示可删数；
- 被引用的图片**列出来**，标注「仍被「XXX」引用，会保留」，复选框禁用；
- 想删掉它，需要先把引用方结束，或去掉引用方里的这张图。

**推广**：任何"数量为 0"的界面，都要区分"真的没有"和"有但被规则挡住"，并写明是哪条规则、被谁挡住。

### 坑 8：清理执行顺序错误 → 用户取消勾选仍被删除

**症状**：取消勾选某张后，列表剔除了它，但执行清理后正文里它也被删了。

**根因**：我按勾选过滤了 `removable` 列表，**却没有把同一范围传给文本摘除**——`buildImageCleanupPlan` 内部按"全部可删"算出了新的正文。

**修法**：给纯函数加 `onlyPaths`（只删这些），调用方把"已勾选的图片"同时用于**删除范围**与**文本摘除**，两者必须同源。

### 坑 9：清理时正文被写空

**症状**：清理后条目正文变成空字符串。

**根因**：清理分了三步写：①摘引用 ②删文件 ③清登记。第③步用了循环外捕获的旧 `item`，把刚写入的正文覆盖回去了。

**修法**：**合并成一次写入**（摘引用 + 清登记同时完成），彻底消除第二个覆盖窗口。原则：**同一个条目在一次操作里只写一次**。

### 坑 10：上传未完成时的竞态

**症状**：
- 上传中失焦保存，会把 `xz-upload://` 占位符（半成品）写进条目；
- 上传中切换到别的条目，迟到的上传结果写进了新条目。

**修法**：
- 上传中标记状态，保存前检查"还有未完成占位符"就拒绝写入，并移除未完成占位符；
- 每次上传记录"草稿归属"（`draftKey = 条目id:字段`）与源条目 id，结果回来时比对，不一致直接丢弃。

---

## 四、踩过的坑：与思源交互

### 坑 11：行舟的图片会被思源"清理未引用资源"误删

**事实**（读内核源码确认）：思源判定"未引用"时**只扫描笔记本文档树的链接与题头图**，插件 JSON 里的引用不被识别。思源自己的 AI 会话图片之所以安全，是内核为它写了一段白名单（`kernel/model/assets.go` 的 `agentSessionImageAssetDests`），**不会覆盖第三方插件**。

**后果**：如果图片放在 `assets/` 且在文档里没被引用，用户一旦在思源里执行「清理未引用资源」，行舟的图会被删掉，细则里只剩裂图。

**当前状态**：本版**没有**实现"引用保护"（生成存档文档承载引用），而是靠"完成即清理"让暴露窗口变短，并在清理时以思源的判定为准（不会误删别人的图）。**这是一个已知的剩余风险**，若要彻底解决，需按设计稿方案 A 生成轻量存档文档。

### 坑 12：`readDir` 不返回文件大小

**症状**：想算"资源库总量"占比，目录接口却没有 `size` 字段。

**修法**：列出目录后逐个 `statAsset`，并发上限 16，结果缓存。注意 512 条上限问题只在 `getUnusedAssets` 上（删除时用），不影响列举。

### 坑 13：弹窗宽度不可控

**症状**：确认窗里内容只占左边 2/3，右侧一大片空白；底部文字被截断。

**修法**：**放弃弹窗**，把"逐张勾选确认"做成清理页里的第二步（页内渲染，宽度撑满）。诊断这类问题的有效手段是**量几何**（`getBoundingClientRect`）和**量像素**（解码截图统计颜色分区），而不是反复调 CSS 猜。

### 坑 14：样式改动可能被浏览器缓存

思源的插件 CSS 由内核伺服，用户"重新加载插件"后仍可能拿到旧样式。**排查顺序**：
1. 先确认插件目录里的文件哈希与 `dist/` 一致；
2. 再用像素/几何测量判断"用户看到的到底是什么"，而不是假设它没生效；
3. 最后才怀疑代码。

---

## 五、排查方法论（本版最值钱的经验）

1. **先量化，再改代码**。反复改 CSS 猜"框为什么不对"浪费了多轮；真正解决问题的是：解码原始 PNG 看亮度分布（发现"黑块"是深色主题截图，不是 bug）、量元素几何（发现工具栏按内容收缩）、量像素分区（发现浅灰底已删除）。
2. **先看数据，再看代码**。用户在清理页看到"0 张"时，第一步应该是查 `work-items.json` 的真实字段值；结论马上出来（另一条未结束条目引用了同一张图）。
3. **纯函数 + 显式传参**。所有排期/过滤/统计逻辑都放纯函数，用单测覆盖；组件只负责取值与渲染。本版所有真 bug 都能在纯函数层复现。
4. **每个 bug 都补回归测试**，尤其是竞态、状态回推、字段默认值这类"看起来已修好"的问题。
5. **测试断言要断言"用户可见的结果"**（按钮文案、DOM 结构、写库内容），而不是断言内部状态。

---

## 六、已知剩余风险与后续可做的事

| 项 | 说明 | 建议 |
|---|---|---|
| 思源清理误删 | 行舟图片只被插件 JSON 引用，思源可能判为未引用 | 实现设计稿方案 A：为含图条目生成轻量"存档文档"承载引用 |
| 清理历史 | 目前只显示本次结果，没有持久化的历史记录 | 需要新增清理记录存储与展示 |
| 图库总量统计成本 | 资源库总量要逐个 `statAsset`，资源多时较慢 | 可加时间戳缓存（如 24 小时内不重扫） |
| 缩略图性能 | 原分辨率大图在列表里直接加载 | 可改为懒加载 + 生成缩略图缓存 |
| 手动上传的图无 `xz-` 前缀 | 用户手动上传的图靠"细则里出现过"识别，命名前缀只作辅助 | 清理范围已按"细则里出现的路径"判定，勿改成只看前缀 |

---

## 七、相关文件索引

| 文件 | 职责 |
|---|---|
| `src/action-images.ts` | 纯函数：图片语法解析、光标插入/移除、占位符、内容指纹、字节读取回退 |
| `src/asset-upload.ts` | 思源资源 API：上传、体积查询、未引用资源列表、单张删除、资源库总量 |
| `src/action-image-cleanup.ts` | 纯函数：登记计划、到期/倒计时、清理范围计算、图库体检统计 |
| `src/XingzhouApp.svelte` | 清理页两步流程、体检区块、粘贴/拖拽上传、角标与确认步骤 |
| `src/index.scss` | 相应样式（入口按钮、清理页、确认步骤、体检区块；均含容器查询窄屏适配） |
| `tests/action-images.test.ts`、`tests/action-image-cleanup.test.ts`、`tests/asset-upload.test.ts`、`tests/action-image-paste.test.ts` | 对应测试（共约 60 个用例覆盖这条功能线） |

---

*生成于 v2.0.0 发布时（2026-09-10），基于本次开发全过程的实测记录。*
